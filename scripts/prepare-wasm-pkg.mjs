import { existsSync, rmSync, writeFileSync } from 'node:fs';

const pkgGitignorePath = 'wasm/pkg/.gitignore';

if (existsSync(pkgGitignorePath)) {
  rmSync(pkgGitignorePath);
}

writeFileSync(
  'wasm/pkg/.keep',
  'Generated by wasm-pack. This folder is intentionally versioned.\n',
  'utf8',
);
